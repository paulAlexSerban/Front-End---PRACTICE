{
  "name": "ui-websote-boilerplate",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo 'test scripts not defined'",
    "preversion": "git config --global push.followTags false",
    "postversion": "git tag -d v$(jq -r .version package.json) && git tag ui-website-boilerplate-$(jq -r .version package.json) && git push origin ui-website-boilerplate-$(jq -r .version package.json)"
  },
  "keywords": [],
  "author": "Paul Serban",
  "license": "ISC",
  "dependencies": {
    "webpack": "^5.88.2"
  }
}
